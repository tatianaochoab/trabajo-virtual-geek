//----------A LA CARRERA----------
//----METODOS --> Son definidos por funciones anonimas-----

const geekGirl1 = {
    name: 'Susana',
    age: '34 años',
    profession: 'Periodista',
};
run = (accion) => `Estoy ${accion}`;
runAMaraton = (distance) => ` un maratón de ${distance}`

console.log(run('corriendo') + runAMaraton('50 Kilometros'));

